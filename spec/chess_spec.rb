require "rspec"
#require File.expand_path(File.dirname(__FILE__) + '/../spec/spec_helper.rb')
require "chess"

describe Pawn do
	describe "it moves correctly" do
		it "only moves forward"

		it "can move two spaces on the first move"

		it "can only move one space after the first move"
	end

	describe "it captures correctly" do
		it "can capture diagonally"

		it "cannot capture straight"
	end
end